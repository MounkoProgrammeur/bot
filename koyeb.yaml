name: telegram-bot
services:
  - name: bot
    type: worker
    env:
      - key: TELEGRAM_BOT_TOKEN
        value: ${TELEGRAM_BOT_TOKEN}
      - key: GROQ_API_KEY
        value: ${GROQ_API_KEY}
    build:
      type: docker
      dockerfile: Dockerfile
    run:
      command: python mybot.py
